# PSOP WebSocket 正常关闭恢复

## 目标

任务进行中，如服务端以正常关闭方式结束 WebSocket，客户端仍自动恢复连接并补齐遗漏消息。

## 规则

- `onFailure` 与 `onClosed` 均进入同一重连流程。
- 每条连接使用独立令牌；旧连接的回调不得改变新连接状态或触发重连。
- 用户主动退出、切换任务或任务结束时，使旧令牌失效，不进行重连。
- 重连前保留现有任务状态检查；任务已结束则停止重连。
- 已接收序号保持不变，复用现有补拉接口恢复遗漏事件。
